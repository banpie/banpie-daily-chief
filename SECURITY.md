# 安全政策

## 支持版本

安全修复优先覆盖最新预览版和最新稳定版。预览版 API 可能变化，但不会放宽本地监听、令牌和外部只读边界。

## 报告漏洞

请通过 GitHub Security Advisory 私密报告，不要在公开 Issue 中提交令牌、邮件、任务数据库或个人路径。报告应包含受影响版本、复现步骤、影响和最小脱敏证据。

## 安全不变量

- 服务只能绑定 `127.0.0.1`。
- API 请求必须携带当前进程生成的随机令牌。
- 插件不得修改宿主的模型、鉴权或生命线运行配置。
- 外部数据源默认只读；本地写能力仅限 `local.tasks.write`。
- 源码、夹具、日志和 Release 不得含凭证、个人数据库或用户路径。

